import 'package:flutter/material.dart';

import 'frinds_scren.dart';
import 'home_Scren.dart';

class fispoack extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: frindsScren(),
      debugShowCheckedModeBanner: false,
    );
  }
}