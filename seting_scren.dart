import 'package:flutter/material.dart';

class setingScren extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Frend'),
      ),
      body: Center(
          child: Text('frind Scren')),
    );
  }
}